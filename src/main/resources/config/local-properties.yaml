# Configuration file

api:
  version: "v1"
  
autodiscovery:
  api:
    id: ""
    
http:
  listener:
    path: "/rabbitmq-sa/api/${api.version}"
    timeout: "120000" #2 min
    
    
rabbitmq:
  host: "localhost"
  port: "5672"
  user: "guest"
  password: "guest"